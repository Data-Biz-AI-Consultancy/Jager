# Use the official N8N image as the base
FROM docker.n8n.io/n8nio/n8n:latest

# You can add custom system dependencies, node modules, or python scripts here if needed.
# For example, if you want to use python in execution nodes:
# USER root
# RUN apk add --no-update python3 py3-pip
# USER node
