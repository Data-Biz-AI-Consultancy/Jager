version: 2

models:
  - name: t_jager__cdp__fct_activities
    description: "Presentation model exposing CDP activity facts for application interfaces in t_jager schema."
    columns:
      - name: activity_id
        description: "Primary key for activity record."
        tests:
          - not_null
          - unique
