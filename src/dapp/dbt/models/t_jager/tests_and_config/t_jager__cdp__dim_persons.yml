version: 2

models:
  - name: t_jager__cdp__dim_persons
    description: "Presentation model exposing CDP person dimensions for application interfaces in t_jager schema."
    columns:
      - name: person_id
        description: "Primary key for person entity."
        tests:
          - not_null
          - unique
