version: 2

models:
  - name: t_jager__cdp__dim_companies
    description: "Presentation model exposing CDP company dimensions for application interfaces in t_jager schema."
    columns:
      - name: company_id
        description: "Primary key for company entity."
        tests:
          - not_null
          - unique
