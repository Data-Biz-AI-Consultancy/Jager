version: 2

models:
  - name: t_jager__cdp__fct_leads
    description: "Presentation model exposing CDP lead facts for application interfaces in t_jager schema."
    columns:
      - name: lead_id
        description: "Primary key for lead entity."
        tests:
          - not_null
          - unique
