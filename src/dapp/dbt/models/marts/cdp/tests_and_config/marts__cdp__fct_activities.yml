version: 2

models:
  - name: marts__cdp__fct_activities
    description: "Curated fact table for CDP activities."
    columns:
      - name: activity_id
        description: "Primary key for the activity."
      - name: activity_type
        description: "Type of activity."
