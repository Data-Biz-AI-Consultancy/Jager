version: 2

models:
  - name: staging__cdp__activities
    description: "Staging model for CDP activities table."
    columns:
      - name: activity_id
        description: "Primary key of the activity."
        tests:
          - unique
          - not_null
      - name: activity_type
        description: "Type of activity (e.g. email, call, note, meeting)."
        tests:
          - not_null

