version: 2

models:
  - name: staging__cdp__person_company_relationships
    description: "Staging model for CDP person to company relationships."
    columns:
      - name: relationship_id
        description: "Primary key of the relationship record."
        tests:
          - unique
          - not_null
      - name: job_title
        description: "Job title of the person at the company."
